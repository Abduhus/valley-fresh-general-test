{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs", "yarn"]
        },
        "install": {
          "cmds": ["npm ci", "npm install -g typescript"]
        },
        "build": {
          "cmds": ["npm run build"]
        }
      },
      "providers": ["node"]
    }
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}